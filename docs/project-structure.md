# 项目结构详解

## 📁 根目录结构

```
twinkle-tools-web/
├── .next/                    # Next.js 构建输出目录
├── .gitignore                 # Git 忽略文件
├── bun.lock                   # Bun 锁定文件
├── components.json            # shadcn/ui 配置文件
├── docs/                      # 项目文档目录
├── eslint.config.mjs          # ESLint 配置
├── next.config.ts             # Next.js 配置
├── package.json               # 项目依赖配置
├── postcss.config.mjs         # PostCSS 配置
├── public/                    # 静态资源目录
├── tsconfig.json              # TypeScript 配置
└── app/                       # Next.js App Router 目录
```

### 配置文件说明

#### components.json
shadcn/ui 的配置文件，定义了：
- 组件样式风格 (style: "default")
- TypeScript 支持 (tsx: true)
- Tailwind CSS 配置路径
- 路径别名设置

#### eslint.config.mjs
ESLint 代码检查配置，采用 Next.js 推荐配置。

#### tsconfig.json
TypeScript 编译配置，包含：
- 路径别名设置 (`@/`)
- 严格模式启用
- JSX 转换配置

---

## 📂 app/ 目录详解

### 结构概览
```
app/
├── favicon.ico               # 网站图标
├── globals.css               # 全局样式
├── layout.tsx                # 根布局组件
└── page.tsx                  # 首页组件
```

### layout.tsx - 根布局
```typescript
// 功能：
// 1. 设置 HTML 基础结构
// 2. 配置字体 (Geist Sans, Geist Mono)
// 3. 应用全局样式
// 4. 设置页面元数据
```

### page.tsx - 首页
```typescript
// 功能：
// 1. 首页导航组件
// 2. 工具卡片展示
// 3. 英雄区域
// 4. 特性介绍
// 5. 页脚信息
```

### globals.css - 全局样式
包含：
- Tailwind CSS 基础样式导入
- 自定义 CSS 变量定义
- 全局样式重置

---

## 🧩 components/ 目录详解

### 结构概览
```
components/
├── ui.ts                      # UI 组件导出文件
└── ui/                        # shadcn/ui 基础组件
    ├── badge.tsx               # 徽章组件
    ├── button.tsx              # 按钮组件
    ├── card.tsx                # 卡片组件
    └── navigation-menu.tsx     # 导航菜单组件
```

### 组件层级关系
```
应用层 (app/)
├── 布局组件 (layout.tsx)
└── 页面组件 (page.tsx)
    └── 业务组件
        ├── HeroSection
        ├── ToolsGrid
        └── FeaturesSection
            └── 基础组件 (components/ui/)
                ├── Button
                ├── Card
                ├── Badge
                └── NavigationMenu
```

### UI 组件详解

#### button.tsx
- **功能**: 提供多种样式的按钮
- **变体**: default, destructive, outline, secondary, ghost, link
- **尺寸**: default, sm, lg, icon
- **特性**: 支持自定义 className 和作为子组件

#### card.tsx
- **功能**: 卡片容器组件
- **子组件**: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
- **用途**: 工具卡片展示容器

#### badge.tsx
- **功能**: 小型状态标签
- **变体**: default, secondary, destructive, outline
- **用途**: 工具分类标识

#### navigation-menu.tsx
- **功能**: 复杂的导航菜单系统
- **特性**: 支持下拉菜单、触发器、内容区域
- **用途**: 网站主导航

---

## 📚 public/ 目录详解

### 结构概览
```
public/
├── file.svg                   # 文件图标
├── globe.svg                  # 地球图标
├── next.svg                   # Next.js 标志
├── vercel.svg                 # Vercel 标志
└── window.svg                 # 窗口图标
```

### 用途说明
- 存放不需要构建处理的静态资源
- 可通过根路径直接访问
- 包含项目图标和品牌资源

---

## 📖 docs/ 目录详解

### 文档结构
```
docs/
├── README.md                  # 文档入口
├── overview.md                # 项目概述
├── tech-stack.md              # 技术栈详解
├── project-structure.md       # 项目结构 (本文件)
├── setup.md                   # 环境搭建
├── coding-standards.md        # 开发规范
├── components.md              # 组件库使用指南
├── deployment.md              # 构建部署
├── performance.md             # 性能优化
├── modules/                   # 模块文档
│   ├── homepage.md            # 首页模块
│   ├── tools.md               # 工具模块
│   └── layout.md              # 布局组件
└── api/                       # API 文档 (未来)
    └── tools-api.md           # 工具 API
```

---

## 🔧 配置文件详解

### package.json 脚本说明
```json
{
  "scripts": {
    "dev": "next dev",        // 启动开发服务器
    "build": "next build",    // 构建生产版本
    "start": "next start",    // 启动生产服务器
    "lint": "eslint"          // 运行代码检查
  }
}
```

### Bun 使用
项目使用 Bun 作为包管理器：
- `bun install` - 安装依赖
- `bun run <script>` - 运行脚本
- `bunx <command>` - 运行全局命令

---

## 📁 未来规划目录

### 待创建目录
```
app/
├── calculator/               # 计算器工具页面
├── color-picker/            # 颜色选择器页面
├── text-formatter/          # 文本格式化页面
├── timer/                   # 倒计时器页面
├── image-compressor/        # 图片压缩页面
├── hash-generator/          # 哈希生成器页面
├── qr-code/                 # 二维码生成器页面
├── password-generator/      # 密码生成器页面
├── json-formatter/          # JSON 格式化页面
├── base64/                  # Base64 编解码页面
├── sql-formatter/           # SQL 格式化页面
└── url-encoder/             # URL 编解码页面

components/
├── features/                # 功能组件
│   ├── HeroSection/        # 英雄区域组件
│   ├── ToolsGrid/          # 工具网格组件
│   └── FeaturesSection/    # 特性介绍组件
├── layout/                  # 布局组件
│   ├── Header/             # 头部组件
│   ├── Footer/             # 页脚组件
│   └── Navigation/         # 导航组件
└── tools/                   # 工具组件
    ├── Calculator/          # 计算器组件
    ├── ColorPicker/        # 颜色选择器
    └── ...                 # 其他工具组件

lib/
├── utils.ts                 # 工具函数库
├── constants.ts             # 常量定义
├── types.ts                 # 类型定义
└── styles/                 # 样式工具
    └── cn.ts               # 类名合并工具

hooks/                       # 自定义 Hooks
├── useLocalStorage.ts       # 本地存储 Hook
├── useTheme.ts              # 主题切换 Hook
└── useDebounce.ts          # 防抖 Hook
```

---

## 🗂 文件命名规范

### 组件文件
- **React 组件**: PascalCase (如 `Button.tsx`, `Card.tsx`)
- **页面文件**: page.tsx (App Router 约定)
- **布局文件**: layout.tsx (App Router 约定)
- **加载文件**: loading.tsx (App Router 约定)
- **错误文件**: error.tsx (App Router 约定)

### 其他文件
- **工具库**: camelCase (如 `utils.ts`, `constants.ts`)
- **样式文件**: kebab-case (如 `globals.css`)
- **配置文件**: 配置格式约定 (如 `.eslintrc.json`)

### 目录命名
- **页面目录**: kebab-case (如 `color-picker/`)
- **组件目录**: PascalCase (如 `Calculator/`)
- **功能目录**: camelCase (如 `features/`)

---

## 📦 模块导入规范

### 路径别名
```typescript
// 配置的路径别名
@/           // 项目根目录
@/components/ // 组件目录
@/lib/       // 工具库目录
```

### 导入顺序
1. React 相关导入
2. 第三方库导入
3. 内部组件导入 (使用 @/ 别名)
4. 相对路径导入
5. 类型导入

```typescript
// 示例导入顺序
import React, { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Card, CardContent } from '@/components/ui/card';
import { ToolCard } from './ToolCard';
import type { Tool } from '@/types/tools';
```

---

## 🔄 文件生命周期

### 开发阶段
1. 创建组件/页面文件
2. 编写组件逻辑
3. 添加样式和交互
4. 编写测试 (未来)
5. 更新文档

### 构建阶段
1. Next.js 分析依赖关系
2. TypeScript 编译检查
3. 代码分割和优化
4. 资源打包和压缩
5. 生成静态文件

### 部署阶段
1. 构建产物上传
2. CDN 缓存配置
3. 域名解析设置
4. HTTPS 证书配置
5. 监控和日志配置

---

这个项目结构设计遵循了 Next.js 14 的最佳实践，同时考虑了项目的可扩展性和维护性。所有文件和目录都有明确的职责分工，便于团队协作和项目管理。